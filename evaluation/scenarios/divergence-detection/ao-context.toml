# ao-context.toml
# Agent-generated Team Context Document
# Scenario: divergence-detection
# Source: https://github.com/denisecase/cintel-01-getting-started
# Generated by: claude-sonnet-4-6
# Generated on: 2026-04-04
#
# No ao-config.toml was present in the repository.
# All conventions derived from observed evidence only.
# best_practices_stance explicitly set to "balanced" per scenario.
#
# Scanned sources:
#   pyproject.toml, .pre-commit-config.yaml, .python-version,
#   .github/workflows/ci-python-zensical.yml,
#   src/cintel/pipeline_case.py, src/cintel/__init__.py,
#   README.md, .editorconfig
#
# This is a course scaffold repository. Several divergences from community
# best practices are intentional design choices for a student-facing context
# (minimal dev tooling, no tests required, simplified structure).
# All divergences are recorded regardless of intent — team to review and
# confirm which are deliberate.

[project]
name = "cintel-01-getting-started"
description = "Course scaffold for continuous intelligence: getting-started project confirming environment setup."
since = "2026"
primary_language = "python"

[alignment]
# Explicitly set per scenario instruction.
# Evidence: consistent with observed tooling choices — professional baseline
# (pyproject.toml, pre-commit, ruff, uv, CI) combined with intentional
# omissions appropriate for a student-facing scaffold.
best_practices_stance = "balanced"

[conventions]
# snake_case observed in source file (pipeline_case.py, ROOT_DIR, DOCS_DIR, main()).
# No class definitions present to confirm PascalCase.
naming = "snake_case for files, functions, and variables (observed in pipeline_case.py: ROOT_DIR, DOCS_DIR, main())"

# src layout is present and explicitly configured in pyproject.toml:
# package-dir = { "" = "src" }, with source in src/cintel/.
# Despite scenario hint about possible flat layout, src layout IS observed here.
structure = "src layout: src/cintel/, docs/ (flat: glossary.md, index.md, project-instructions.md, your-files.md); no tests/ directory present"

# ruff via pre-commit and CI; rule set is a strict subset:
# E,F,W,I,UP,B,PTH — no D,S,C4,SIM,RET rules.
# POLICY comment in pyproject.toml: "Docstring rules are NOT enabled."
style_enforced_by = "ruff (format + lint) via pre-commit hooks and GitHub Actions CI (ci-python-zensical.yml)"

# No tests/ directory, no test files, no pytest in dev deps,
# no [tool.pytest.ini_options] in pyproject.toml.
# CI step C1 gracefully skips if no tests/ found.
test_pattern = "no tests present; no pytest configured; CI step C1 skips gracefully when tests/ is absent"

# Observed from README Developer Setup (implicit from pro-analytics-02 workflow guide link).
# Commit style not directly observable from this repo's history.
commit_style = "not enforced; no conventional commits tooling; README links to pro-analytics-02 workflow guide which shows informal 'update' messages"

[code_style]
# Not configured as a limit in pyproject.toml or ruff.
max_function_lines = 0
max_cyclomatic_complexity = 0
max_nesting_depth = 0

# ruff RET rules NOT in select list; cannot confirm from config.
# Observed: main() has no early returns (single-path function).
prefer_early_returns = false

# Google convention declared in [tool.ruff.lint.pydocstyle] with REQ comment,
# but D rules are NOT enabled in ruff lint select.
# Observed in pipeline_case.py: main() has a brief Google-style docstring
# but no Args/Returns/Raises sections (single-sentence only).
docstring_style = "google (declared in pyproject.toml pydocstyle convention, but D lint rules not enforced)"

# D rules explicitly disabled by POLICY comment in pyproject.toml.
# Observed: module has a module-level docstring; main() has a one-line docstring.
# No enforcement — docstrings present but not required by tooling.
docstring_required_on = "not enforced by tooling (D rules not in ruff lint select per POLICY comment); module-level and main() docstrings present by convention only"

# Some type annotations observed: LOG: logging.Logger, ROOT_DIR: Path,
# DOCS_DIR: Path, main() -> None. No type checker configured.
# No pyright or mypy in dev deps. No [tool.pyright] section.
type_annotations = "partial; module-level constants and main() return type annotated; no type checker configured (pyright/mypy absent from dev deps and pyproject.toml)"

# REQ/WHY/OBS comment pattern observed in pyproject.toml and CI workflow.
# Source file uses === section headers instead of REQ/WHY comments.
comment_style = "section headers (=== SECTION ===) in source file; REQ:/WHY:/OBS: pattern in pyproject.toml and CI workflow"

# ruff isort (I rules) selected; force-sort-within-sections = false.
# (Other repos in this ecosystem use true; minor divergence noted in divergences.)
import_order = "stdlib, third-party, local; ruff isort (I rules); force-sort-within-sections = false"

[python]
package_manager = "uv"
layout = "src"
# Static version = "0.1.0"; no setuptools_scm; not published to PyPI.
build_backend = "setuptools (static version = '0.1.0'; no setuptools_scm; not a PyPI-published package)"
formatter = "ruff format"
linter = "ruff check"
# No type checker configured — pyright/mypy absent from dev deps and pyproject.toml.
type_checker = "none configured"
# No pytest or [tool.pytest.ini_options] present.
test_runner = "none configured"
ci = "github-actions"

[done]
# Derived from CI workflow (ci-python-zensical.yml): pre-commit, validate-pyproject,
# and Zensical docs build. No test step runs (tests/ absent).
definition = "ruff format clean, ruff lint clean, pre-commit hooks pass, validate-pyproject clean, Zensical docs build succeeds; no test requirement (tests/ absent)"
review_required = ""
ci_must_pass = true
# deploy-zensical.yml present for GitHub Pages.
deploy_process = "merge to main triggers GitHub Pages docs deploy via deploy-zensical.yml"

[ownership]
# Course scaffold by Denise Case. Solo project for course use.
# Students fork/copy and own their own versions.

[norms]
# Observed in pyproject.toml, CI workflow, and pre-commit config.
req_why_obs_pattern = "REQ:/WHY:/OBS: prefix pattern used in pyproject.toml and CI workflow; source file uses section-header style (=== SECTION ===)"

# Observed from README and pyproject.toml.
uv_primary = "uv is the primary package manager for all installs and command execution"

# Observed from CI ci-python-zensical.yml step labeling.
abcd_ci_ordering = "CI/CD steps labeled A (Assemble), B (Baseline), C (Coverage/Tests), D (Deploy)"

# Explicitly stated in scenario prompt and observed from repo structure.
course_scaffold = "intentionally minimal; designed for students to clone, run, and customize; not a production or PyPI-published package"

# Observed: src/cintel/ has only pipeline_case.py and __init__.py.
# Minimal source for a getting-started exercise.
minimal_source = "single pipeline script (pipeline_case.py); one-file implementation appropriate for getting-started scope"

[tools]
registry = ""

[meta]
produced_by = "claude-sonnet-4-6"
produced_on = "2026-04-04"
refreshed_on = ""
gaps = [
  "commit_style: no direct evidence in this repo; README links to external workflow guide (pro-analytics-02); cannot confirm from observed files",
  "max_function_lines: not configured; observed function (main()) is ~15 lines but single example is not a pattern",
  "naming (PascalCase for classes): no class definitions in source; convention not directly observable",
  "CHANGELOG.md: not present; change history not tracked in this repo",
  ".github/dependabot.yml: not present; automated dependency updates not configured",
  "force-sort-within-sections = false: observed value; other repos in this ecosystem use true; unclear if intentional deviation or template inconsistency",
  "SE_MANIFEST.toml: not present (present in datafun-toolkit and MEE); may be intentionally omitted for course scaffolds",
  "tests/: no test directory or test files present; no pytest configuration",
  "type_checker: no pyright or mypy in dev deps and no config block; strictness level cannot be determined",
]
inferred = [
  "since: '2026' inferred from pipeline_case.py module docstring 'Date: 2026-03-07'",
  "course_scaffold intent: inferred from README content ('This is the getting started project'), minimal dev deps, and scenario prompt description",
  "src layout: observed from pyproject.toml package-dir config; scenario prompt mentioned possible flat layout but src layout is actually present",
  "prefer_early_returns: false inferred from ruff RET rules absent from lint select; single observed function has no early returns",
  "commit_style: inferred by association — repo links to pro-analytics-02 workflow guide which shows 'update' style commits",
  "best_practices_stance: 'balanced' explicitly specified by scenario; consistent with observed pattern of applying professional tooling baseline while omitting advanced features for student audience",
]
divergences = [
  "no_tests [observed: no tests/ directory or pytest; best practice: automated tests with pytest and coverage]: CI gracefully skips if tests/ absent; by design for a getting-started scaffold; flagged for awareness",
  "no_type_checker [observed: no pyright or mypy in dev deps, no [tool.pyright] config; best practice: pyright strict or mypy for static type checking]: partial type annotations present in source but not checked; by design for student scaffold; flagged for awareness",
  "docstring_D_rules_disabled [observed: D rules absent from ruff lint select; POLICY comment confirms intent; best practice: ruff D rules or pydocstyle to enforce docstring coverage]: module and main() have docstrings but enforcement absent; by design; flagged for awareness",
  "no_security_rules [observed: S (security) rules not in ruff lint select; no bandit in dev deps; best practice: ruff S rules or bandit for security scanning]: not enforced in this repo; by design for minimal scaffold; flagged for awareness",
  "no_C4_SIM_RET_rules [observed: C4 (comprehensions), SIM (simplify), RET (returns) not in ruff lint select; best practice: these rules catch common code quality issues]: absent from rule set; narrower than professional baseline; flagged for awareness",
  "no_changelog [observed: CHANGELOG.md not present; best practice: Keep a Changelog format]: change history not maintained; by design for course scaffold; flagged for awareness",
  "no_dependabot [observed: .github/dependabot.yml not present; best practice: Dependabot for automated dependency security updates]: not configured; flagged for awareness",
  "ruff_target_version_mismatch [observed: target-version = 'py313' in [tool.ruff]; but requires-python = '>=3.14' and .python-version = '3.14'; best practice: ruff target-version should match the actual minimum Python version]: same inconsistency observed across other denisecase repos; team-wide pattern",
  "force_sort_within_sections_false [observed: force-sort-within-sections = false; other repos in this ecosystem (datafun-toolkit, MEE) use true; best practice: true for deterministic import ordering within sections]: minor deviation from org pattern; may be intentional or a template inconsistency",
  "no_pytest_in_dev_deps [observed: pytest, pytest-cov, pyright absent from [project.optional-dependencies].dev; best practice: test runner and type checker in dev deps]: dev deps limited to packaging, pre-commit, ruff, validate-pyproject; by design for minimal scaffold",
]
