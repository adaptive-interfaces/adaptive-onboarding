# .agent/ao-config-python.toml
# Adaptive Onboarding (AO)
# Team-Provided Input Configuration
# Python Layer
# https://github.com/adaptive-interfaces/adaptive-onboarding
# Extends ao-config.toml for all Python repositories.
# Applied after ao-config.toml; overrides org-wide defaults where specified.
# Further extended by ao-config-python-pypi.toml or ao-config-python-scaffold.toml
# when repo type applies.

[defaults.python]
package_manager = "uv"
layout = "src"
build_backend = "hatchling"
formatter = "ruff format"
linter = "ruff check"
type_checker = "pyright strict"
test_runner = "pytest"
ci = "github-actions"
requires_python = ">=3.14"
confirmed = true       # do not re-infer; accept as known

[defaults.python.testing]
coverage_tool = "pytest-cov"
coverage_threshold = 80           # minimum coverage %; 0 = not enforced
test_location = "tests/"          # mirrors src/ structure
test_naming = "test_<module>.py; test_<function>_<condition>"
fixtures_over_setup = true        # WHY: Prefer pytest fixtures over setUp/tearDown
no_test_interdependence = true    # WHY: Tests must not depend on execution order
confirmed = false

[defaults.python.code_style]
max_function_lines = 30
max_cyclomatic_complexity = 5
max_nesting_depth = 3
docstring_style = "google"
docstring_required_on = "all public functions and classes"
type_annotations = "required on all public functions; pyright strict"
import_order = "stdlib, third-party, local; absolute imports only"
no_wildcard_imports = true
all_exports_explicit = false      # set true if __all__ required in all modules
confirmed = false

[defaults.python.structure]
naming = "snake_case files/functions/variables; PascalCase classes"
structure = "src layout: src/<package>/, tests/, docs/"
style_enforced_by = "ruff"
test_pattern = "tests/ mirrors src/; test_<module>.py; test_<function>_<condition>"
py_typed_marker = false           # set true for published packages
confirmed = false

[defaults.python.security]
bandit_enabled = true
bandit_severity_threshold = "medium"  # low | medium | high
deptry_enabled = true                 # WHY: Check for undeclared or unused dependencies.
confirmed = false

[defaults.python.docs]
doc_tool = "zensical"
doc_output = "site/"
api_docs_autogenerated = true
confirmed = false

[defaults.python.norms]
uv_only = "do not use pip directly; all installs via uv"
no_mutable_defaults = "never use mutable default arguments; use None and assign in body"
logging_style = "use standard logging module; configure at app boundary not library"
environment_vars = "use os.environ with explicit defaults; no dotenv in production code"
exception_style = "raise specific exceptions; include context message; use 'from' chaining"
